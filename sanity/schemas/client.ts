export const client = {
    name: "client",
    type: "document",
    title: "Client",
    fields: [
        {
            name: "name",
            type: "string",
            title: "Name",
            unique: true
        }
    ]
}