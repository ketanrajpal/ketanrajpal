export const tag = {
    name: "tag",
    type: "document",
    title: "Tag",
    fields: [
        {
            name: "name",
            type: "string",
            title: "Name",
            unique: true
        }
    ]
}